<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="?attr/colorSurfaceVariant" />

    <padding
        android:bottom="@dimen/menu_padding"
        android:top="@dimen/menu_padding" />

    <corners
        android:bottomLeftRadius="@dimen/rounded_corners_menu"
        android:bottomRightRadius="@dimen/rounded_corners_menu"
        android:topLeftRadius="@dimen/rounded_corners_menu"
        android:topRightRadius="@dimen/rounded_corners_menu" />

</shape>